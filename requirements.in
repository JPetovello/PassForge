# Direct application dependencies. Keep these aligned with the deployed,
# known-good baseline; generate requirements.lock after intentional changes.
Flask==3.1.3
requests==2.34.2
Flask-Limiter==4.1.1
redis==8.1.0
gunicorn==26.2.0
